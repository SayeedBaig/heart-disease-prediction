# =============================================================================
# CardioAI — Production Runtime Requirements
# Generated: 2026-07-07
#
# Cleaned of development tools, notebooks, and unused plotting libraries.
# Compatible with Python 3.11 - 3.13
# =============================================================================

# ── Web Framework & API ───────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
starlette>=0.27.0
httpx>=0.25.0

# ── Pydantic & Validation ─────────────────────────────────────────────────────
pydantic>=2.4.0
pydantic-settings>=2.0.0
email-validator>=2.0.0

# ── Database — ORM & Migrations ───────────────────────────────────────────────
sqlalchemy>=2.0.0
alembic>=1.11.0
psycopg[binary]>=3.1.9

# ── Environment & Utils ───────────────────────────────────────────────────────
python-dotenv>=1.0.0
aiosmtplib>=3.0.0
reportlab>=4.0.0

# ── ML Inference — Clinical Agent (Stacking Model) ────────────────────────────
scikit-learn>=1.3.0
lightgbm>=4.0.0
xgboost>=2.0.0
joblib>=1.3.0
numpy>=1.26.0
pandas>=2.0.0

# ── ML Inference — ECG Agent (PyTorch) ────────────────────────────────────────
torch>=2.0.0

# ── ML Inference — Echo Agent (TensorFlow / MobileNetV2) ──────────────────────
tensorflow>=2.15.0
opencv-python-headless>=4.8.0

# ── RAG Pipeline — Vector Search & Indexing ───────────────────────────────────
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2
groq>=0.9.0
pymupdf>=1.23.0
